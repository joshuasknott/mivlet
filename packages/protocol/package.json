{
  "name": "@fable/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check:spine-parity": "npm run build && node scripts/check-spine-parity.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
