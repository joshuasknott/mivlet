FROM docker.io/cloudflare/sandbox:next

# Fable's first hosted node is intentionally lean. Provider credentials are
# injected through managed runtime bindings later, never baked into this image.
WORKDIR /workspace
