#!/bin/bash
set -euo pipefail

exec python3 -c 'from pathlib import Path; import urllib.request; token = Path("/run/fable-private/gateway-token").read_text().strip(); urllib.request.urlopen("http://127.0.0.1:9223/" + token + "/status", timeout=3).read()' >/dev/null 2>&1
