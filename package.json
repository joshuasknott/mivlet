{
  "name": "praxis",
  "version": "0.1.0",
  "private": true,
  "description": "Praxis is an AI-native workspace for tools, knowledge, memory, and work.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @praxis/desktop",
    "build": "npm run build --workspace @praxis/protocol && npm run build --workspace @praxis/connectors && npm run build --workspace @praxis/desktop",
    "typecheck": "npm run build --workspace @praxis/protocol && npm run typecheck --workspace @praxis/connectors && npm run typecheck --workspace @praxis/desktop",
    "test": "npm run test --workspace @praxis/desktop",
    "lint": "npm run lint --workspaces --if-present",
    "tauri:check": "cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "tauri:build": "npm run tauri --workspace @praxis/desktop -- build",
    "check": "npm run typecheck && npm run test && npm run build && npm run tauri:check"
  },
  "engines": {
    "node": ">=22",
    "npm": ">=11"
  }
}
